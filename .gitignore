# python project: virtualenvs and caches stay out. uv.lock IS committed -- it is the
# lock of record for deterministic installs (fleet pattern). pipeline data lives under
# ~/var/neal (outside this tree) and never lands here.
.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
.coverage
.DS_Store

# local building aid: the vision/why-context. faithful and personal, kept out of git.
VISION.md
